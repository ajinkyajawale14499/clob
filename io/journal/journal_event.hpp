#pragma once

// The canonical definition lives in core/events/ — see the comment in
// core/events/order_event.hpp. This header exists for the path the plan
// referenced; new code should include core/events/order_event.hpp directly.
#include "core/events/order_event.hpp"
