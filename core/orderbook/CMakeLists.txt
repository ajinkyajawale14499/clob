add_library(clob_orderbook STATIC book.cpp)
target_include_directories(clob_orderbook PUBLIC ${CMAKE_SOURCE_DIR})
target_link_libraries(clob_orderbook PUBLIC clob::types)
target_compile_features(clob_orderbook PUBLIC cxx_std_23)
add_library(clob::orderbook ALIAS clob_orderbook)
